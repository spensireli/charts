apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vrising-chart.fullname" . }}-test-connection"
  labels:
    {{- include "vrising-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vrising-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
